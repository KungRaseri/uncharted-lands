# =============================================================================
# Uncharted Lands - Client Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# IMPORTANT: Use PUBLIC_ prefix for variables accessible in the browser
# SvelteKit requires PUBLIC_ prefix for client-side environment variables

# REST API base URL (used by both client and server-side code)
# Development: http://localhost:3001/api
# Production: https://your-server.onrender.com/api or https://your-server.railway.app/api
PUBLIC_API_URL=http://localhost:3001/api

# WebSocket server URL (used for Socket.IO real-time connections)
# Development: http://localhost:3001 (Socket.IO auto-upgrades to WebSocket)
# Production: https://your-server.onrender.com (use https://, Socket.IO handles wss://)
PUBLIC_WS_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Drizzle ORM
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example (local): postgresql://postgres:password@localhost:5432/uncharted-lands
# Example (Neon): postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/uncharted-lands?sslmode=require
# Example (Supabase): postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:password@localhost:5432/uncharted-lands"

# Optional: Direct database URL (without connection pooling)
# Some providers (like Neon) provide separate URLs for pooled and direct connections
# Use this for migrations if your provider requires it
# DIRECT_URL="postgresql://postgres:password@localhost:5432/uncharted-lands"

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# Node environment
# Options: development, production, test
# Vercel/Netlify set this automatically
NODE_ENV=development

# -----------------------------------------------------------------------------
# Sentry Configuration (Error Tracking & Monitoring)
# -----------------------------------------------------------------------------
# Optional: Get these from https://sentry.io/settings/projects/
# Set to "disabled" if you don't want error tracking

# Sentry DSN (Data Source Name) for browser error reporting
# MUST use PUBLIC_ prefix because it's used in client-side code (hooks.client.ts)
# Example: https://abc123@o123456.ingest.sentry.io/123456
PUBLIC_SENTRY_DSN=disabled

# Sentry Auth Token for source map uploads during build
# Only needed in CI/CD or when deploying with source maps
SENTRY_AUTH_TOKEN=disabled

# -----------------------------------------------------------------------------
# Development Features (Optional)
# -----------------------------------------------------------------------------
# Enable faster tick rates for testing (not yet implemented)
# Set to "true" to speed up resource generation during development
# DEV_TICK_FAST=false

# -----------------------------------------------------------------------------
# CI/CD Configuration
# -----------------------------------------------------------------------------
# These are automatically set by CI systems, no need to set manually
# CI=true
# GITHUB_ACTIONS=true

# -----------------------------------------------------------------------------
# Production Deployment Notes
# -----------------------------------------------------------------------------
# Vercel:
#   - Set PUBLIC_API_URL to your server URL (e.g., https://server.onrender.com/api)
#   - Set PUBLIC_WS_URL to your server URL (e.g., https://server.onrender.com)
#   - Set API_URL to the same as PUBLIC_API_URL
#   - Set DATABASE_URL if using Vercel Postgres or external DB
#   - Add these in: Settings > Environment Variables
#
# Netlify:
#   - Similar to Vercel
#   - Add in: Site settings > Environment variables
#
# IMPORTANT: Make sure your server's CORS_ORIGINS includes your client URL!
# Example: CORS_ORIGINS=https://your-app.vercel.app
# =============================================================================
