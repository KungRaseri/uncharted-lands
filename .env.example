# =============================================================================
# Uncharted Lands - Client Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# URL to the game server's REST API
# Development: http://localhost:3001/api
# Production: https://your-server.com/api
PUBLIC_API_URL=http://localhost:3001/api

# WebSocket URL (for Socket.IO connection)
# Development: ws://localhost:3001 or http://localhost:3001
# Production: wss://your-server.com or https://your-server.com
PUBLIC_WS_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example (local): postgresql://postgres:password@localhost:5432/uncharted_lands
# Example (production): Use connection pooling URL from your provider
DATABASE_URL="postgresql://postgres:password@localhost:5432/uncharted-lands"

# Optional: Direct database URL (without connection pooling)
# Used for migrations. If not set, DATABASE_URL will be used for everything.
# DIRECT_URL="postgresql://postgres:password@localhost:5432/uncharted_lands"

# -----------------------------------------------------------------------------
# Sentry Configuration (Error Tracking & Monitoring)
# -----------------------------------------------------------------------------
# Get these from: https://sentry.io/settings/projects/
# Leave as "disabled" in development if you don't want error tracking

# Sentry DSN (Data Source Name) for error reporting
SENTRY_DSN="disabled"

# Sentry Auth Token for source map uploads during build
# Only needed in CI/CD or when deploying
SENTRY_AUTH_TOKEN="disabled"

# -----------------------------------------------------------------------------
# Development Configuration
# -----------------------------------------------------------------------------
# Node environment (automatically set by most tools)
# NODE_ENV=development|production

# Enable faster tick rates for testing (optional)
# Set to "true" to speed up resource generation during development
# DEV_TICK_FAST=false

# -----------------------------------------------------------------------------
# CI/CD Configuration
# -----------------------------------------------------------------------------
# These are automatically set by CI systems, no need to set manually
# CI=true
# GITHUB_ACTIONS=true
