{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "buildCommand": "npm run build --workspace=server",
  "devCommand": "npm run dev --workspace=server -- --port $PORT",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "ignoreCommand": "bash -c 'if [ -n \"$VERCEL_GIT_COMMIT_REF\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"development\" ]; then exit 1; else git diff --quiet HEAD^ HEAD -- server/; fi'",
}
